name: Spatial terminal I/O check

on:
  push:
    branches:
      - nolane/spatial-agent-shell
    paths:
      - .github/workflows/spatial-terminal-io-check.yml

permissions:
  contents: read

jobs:
  terminal-io-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 24.9.0
          cache: pnpm
      - name: Install immutable workspace
        run: pnpm install --frozen-lockfile
      - name: Verify terminal interaction ownership seam
        run: pnpm exec vitest run packages/api/session-controller/tests/control-terminal.host.spec.ts
