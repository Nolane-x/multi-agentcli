name: CI runner bootstrap

on:
  push:
    branches:
      - nolane/spatial-agent-shell-v2

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Bootstrap no-op
        run: echo "runner bootstrap retired; PR CI uses hosted runners"
